#!/bin/bash
set -euo pipefail
: "${username:=alan}"
: "${password:=demo}"
: "${stalwart:=https://stalwart.opencloud.test}"

exec ${HTTP-http} --follow --max-redirects=2 --verify=no -a "${username}:${password}" "${stalwart}/.well-known/jmap"
